node_modules
.env
dist
.vercel
.env*.local

# Vendored SDK — dist is the shipped artifact, must be committed
!vendor/**/dist
!vendor/**/dist/**

# Mobile (Expo)
mobile/.expo/
mobile/dist/
mobile/ios/
mobile/android/
mobile/web-build/
mcp/.env
.env.production
